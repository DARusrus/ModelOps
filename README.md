# ModelOps

ML Experiment, Model Card & Readiness Assistant.

## Setup

1. Copy `.env.example` to `.env.local` and add your API keys.
2. Run `npm install`
3. Run `npm run dev`

## Architecture

See `docs/architecture.md` for full details.
