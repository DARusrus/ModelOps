export default function RunComparison({ runs }: { runs: any[] }) {
  return (
    <div className="p-4 border rounded mt-4">
      <h3>Run Comparison</h3>
      {/* Render run comparison */}
    </div>
  );
}
